# Evidence RAG 领域上下文

Evidence RAG 的核心不是“能聊天”，而是让一次检索增强回答可以被复现、解释和评估：问题进入检索链路，证据被排序并标识，回答引用证据，离线标注集验证排序质量。

## 语言

### 知识库与资料

**知识库**：一个隔离的资料集合及其索引，回答、文档和评测都在知识库范围内发生。
_避免_：会话（会话是交互记录，不等同于资料集合）

**资料**：用户上传并纳入知识库的原始文件。
_避免_：文档（“文档”只在讨论解析后的内容单元时使用）

**片段**：从资料解析、切分后进入索引的可检索内容单元；片段拥有稳定 ID 和来源定位。
_避免_：文本块、chunk（代码内部可使用 chunk，但领域文档优先使用片段）

**父片段**：用于生成上下文的较完整内容单元。

**子片段**：用于精确召回的较小内容单元；命中后可以恢复为父片段。

### 检索与评测

**候选召回**：某一种检索器为问题产生的有序候选片段列表。

**检索策略**：产生最终有序证据列表的明确方法，包括 `vector`、`bm25`、`rrf` 和 `rerank`。
_避免_：算法、模式（除非讨论实现细节）

**检索降级**：某个检索步骤失败或不可用时，系统按预先声明的替代策略继续返回证据或明确的无证据结果。
_避免_：静默失败

**标注问题集**：由问题、知识库、相关片段和相关性等级组成的版本化评测数据集。
_避免_：测试问题、样例问题（它们不一定有人工相关性标注）

**评测运行**：在固定数据集、切点和检索配置下产生的一份可追溯结果，包含配置指纹、每题结果和汇总指标。

**相关性等级**：标注者对某片段回答问题的有用程度评分；0 表示不相关，正数表示相关程度，等级越高越有用。

**证据**：本次回答实际交给生成模型的有序父片段集合。证据不是全部候选召回结果。

### 回答可信度

**引用校验**：检查回答中的 `[S#]` 标记是否指向本次交给模型的证据；它验证结构完整性，不声称完成语义事实核验。
_避免_：事实正确性证明

**检索轨迹**：一次查询中各候选召回、融合、重排、降级和最终证据的结构化记录。
_避免_：日志（日志是运维输出，轨迹是用户可解释的领域结果）

**索引链路**：资料从上传、解析、切分、写入索引到可检索的有序生命周期。
_避免_：上传状态（上传状态只描述链路中的一个观察面）
